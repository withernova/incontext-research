# R-004 · padding_expand

## 状态
draft

## Variant
padding_expand

## 目的
E-001 draft Run；仅等待用户审核，不自动执行正式 Run。用于观察边界随 padding 扩展的响应。

## 相对基线改动
相对 R-001，不改像素，只把 visual prompt reference boxes 每边扩展 16px；保存图像路径与 intervention_records。

## Seed
（不适用）

## Workspace
W-01

## Git commit / branch
- branch: experiment/E-001-containerization
- commit: 70b702765f2b709679b76eb7489d5cf37330b428

## 配置/超参数
- intervention: padding_expand
- patch_size: 28（shuffle runs）
- padding_pixels: 16（padding_expand）
- padding_ratio: 0.0（padding_expand）
- 数据与模型占位符仍待用户/工具替换：`<MODEL>`, `<VISUAL_PROMPT_JSONL>`, `<IMAGE_ROOT>`, `<N>`

## 运行命令
```bash
cd /home/featurize/work/mechanism/Rex-Omni && mkdir -p /home/featurize/work/mechanism/explog/E-001/R-004/logs /home/featurize/work/mechanism/explog/E-001/R-004/outputs && python evaluation/inference_visual_prompt.py --model_path <MODEL> --test_jsonl_path <VISUAL_PROMPT_JSONL> --image_root_dir <IMAGE_ROOT> --save_path /home/featurize/work/mechanism/explog/E-001/R-004/outputs/predictions.jsonl --start_idx 0 --end_idx <N> --intervention padding_expand --padding_pixels 16 --padding_ratio 0.0 --save_intervention_images_dir /home/featurize/work/mechanism/explog/E-001/R-004/outputs/intervention_images 2>&1 | tee /home/featurize/work/mechanism/explog/E-001/R-004/logs/inference.log
```

## 预期结果
生成 visual-prompt predictions.jsonl；每行包含 extracted_predictions、gt、question、raw_response、intervention、intervention_records，以及可选 intervention_image_paths。

## 验收标准
命令完成且 predictions.jsonl 非空；parse failure 需从 raw_response / extracted_predictions 审查；四个 Runs 需使用同一模型、同一 jsonl、同一 start/end 区间。

## 预期产物
- log: /home/featurize/work/mechanism/explog/E-001/R-004/logs/inference.log
- predictions: /home/featurize/work/mechanism/explog/E-001/R-004/outputs/predictions.jsonl
- intervention images: /home/featurize/work/mechanism/explog/E-001/R-004/outputs/intervention_images/（除 baseline 可为空）

## 前置依赖
- 远程 repo 可访问；已验证。
- `/home/featurize/data` 当前缺失，需确认实际数据路径。
- base Python 环境缺少 `qwen_vl_utils`，需运行前激活/安装正确环境。

## 关键指标/结果
（正式 Run 未执行；待运行后填写）

## 备注
实现验证：`python -m py_compile rex_omni/interventions.py evaluation/inference_visual_prompt.py` 通过；直接加载 `rex_omni/interventions.py` 的 PIL smoke test 通过。未执行模型推理。
