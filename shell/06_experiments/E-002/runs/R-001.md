# R-001 · POIL dataset acquisition / format inspection

## 状态
draft

## 目的
获取或构造 Personalize / POIL 数据 manifest，检查图像路径、reference frames、positive query、negative query、bbox、class/subclass/instance 标识和 N-shot 设置。

## 不做什么
本 run 不跑模型，不做结论；只确认数据协议与最小可运行样本。

## 预期产物
- `shell/06_experiments/E-002/dataset_notes.md`
- `/home/featurize/work/mechanism/explog/E-002/dataset_manifest_preview.jsonl`
- `/home/featurize/work/mechanism/Rex-Omni/tmp/e002/build_or_inspect_poil_manifest.py`

## 待确认
- 官方 `iplocid` 仓库是否可访问；
- 是否已有预构造 manifest；
- LaSOT/VastTrack in-class negative 是否可先小规模获取。
