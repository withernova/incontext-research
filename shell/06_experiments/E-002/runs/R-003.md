# R-003 · Prompt-only self-posed identification baseline

## 状态
draft

## 目的
在不 fine-tune 的情况下，尝试 IPLoc-ID 风格 `bbox + self-posed query + Yes/No` 输出，检查 prompt-only 是否能减少 negative query false positives。

## 关键风险
E-001 的 R-005b/R-005c 已显示 Rex-Omni zero-shot candidate-ID/verification 输出不稳定，因此该 run 主要用于暴露 task-interface mismatch，不应过度解释。

## 前置依赖
R-001 完成并产出可用 manifest；R-002 baseline 完成。
