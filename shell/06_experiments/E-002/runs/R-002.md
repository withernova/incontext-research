# R-002 · Rex-Omni localization-only POIL baseline

## 状态
draft

## 目的
在 POIL positive/negative query 上运行 Rex-Omni localization-only visual prompt baseline。对所有 query 都尝试输出 bbox，以测量 localization-only 方法的 false-positive 行为。

## 指标
- positive mIoU
- negative valid-box rate / false-positive rate
- balanced F1（valid bbox interpreted as positive）

## 前置依赖
R-001 完成并产出可用 manifest。
