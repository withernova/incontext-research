# 003 · query-span attention 置换的自然生成 IoU 对照

- workflow: v2 / ready_to_run / 等待执行授权
- review_status: approved
- group_id: 未分组
- execution_dispatch: dispatch-4c80b78dceb8df3b88c12bbd / queued

## 1. 研究设计
### 研究问题
在相同111个预固定positive样本上，模型逐token自然生成bbox时，仅置换main4 heads的query-image span attention位置，会使生成bbox的IoU相对baseline与identity改变多少？
### 本轮目的
分别在baseline、identity和query_permuted条件下重新自然生成bbox并计算配对IoU，直接回答Run 002只测NLL而未测定位结果的问题。
### 假设或比较预期
如果query图像中注意力权重与空间位置的对应关系参与坐标生成，query_permuted会相对identity降低full IoU或改变IoU阈值通过率；identity应与baseline逐token一致。
### 数据与主要变量
严格复用Run 002预固定的E003-R-004b positive extreme cohort：archived IoU<0.1的35条与IoU>=0.7的76条，共111条；不根据本次结果增删样本。每条使用同一reference/query、模型、LoRA与max_side=640。

三条件均greedy decoding、相同prompt和最大生成长度。baseline无hook；identity在相同在线生成rows原位写回；query_permuted仅在bbox前缀生成阶段，从首个bbox token预测开始至生成右方括号为止，对L18H15/L19H03/L22H00/L20H08的当前decode query row内query视觉span作确定性双射置换。reference/text/其他heads和值向量不变。

## 2. 指标设计
主指标为每条样本full IoU（无合法bbox记0）及query_permuted-identity的配对IoU差；同时报告mean/median、正负零计数、IoU@0.3/0.5/0.7通过率差、合法bbox解析率、Yes/No变化。identity必须与baseline生成文本、bbox和IoU逐条一致后才解释置换结果。
### Exact teacher-replay通过率 (`exact_replay_rate`)
- 公式：N_exact / N_total
- 含义：归档响应token、bbox p-1 row与双图span均精确对齐的样本比例
- 汇总与范围：micro ratio / R-000工程门禁与R-001完整性

### GT区域fractional attention mass (`fractional_gt_mass`)
- 公式：sum_j A_j * w_j，其中w_j为视觉token与GT bbox的fractional occupancy
- 含义：指定row/head在目标区域内分配的raw attention质量
- 汇总与范围：逐head样本中位数及sequence bootstrap / Q→Q[G]、Q→R[G]、G→R[G]、Q→Q[L]

### GT区域面积归一化富集 (`area_normalized_enrichment`)
- 公式：(sum_j A_j*w_j / sum_j A_j) / (sum_j w_j / N_visual)
- 含义：相对均匀视觉attention时目标区域获得的富集倍数
- 汇总与范围：逐head样本中位数及correct-error差 / 各视觉span内条件化attention

### Raw-attention pointing命中率 (`pointing_rate`)
- 公式：N[argmax_j A_j的token与GT bbox有fractional overlap] / N_eligible
- 含义：raw map峰值是否落入目标区域的样本比例
- 汇总与范围：逐head分子/分母、Wilson区间与sequence bootstrap差 / GT覆盖至少一个merged visual token的样本

### 累计质量fractional-token IoU AUC (`retained_mass_fiou_auc`)
- 公式：mean_{rho in {0.05,...,0.95}} fIoU(S_rho,GT)，S_rho为达到累计attention质量rho的最小token集合
- 含义：衡量distributed attention support与GT token区域在多阈值下的空间重合
- 汇总与范围：逐head样本中位数及sequence bootstrap / 各预注册edge的视觉span条件化raw attention

## 3. 代码架构
复用公共Qwen loader、image span定位和attention rewrite controller；新增natural_iou_permutation_pipeline.py与tools launcher。
- 公共包：`mechanism/iplocid/iplocid`
- 入口：`iplocid.pipelines.natural_iou_permutation_pipeline:main`
- 配置：``
- Shell launcher：`mechanism/iplocid/tools/run_e008_r003.sh`
- 复用模块：mechanism/iplocid/iplocid/attention/rewrite.py, mechanism/iplocid/iplocid/attention/spans.py, mechanism/iplocid/iplocid/models/qwen.py
- 新增模块：（待登记）
- 测试：python -m py_compile, python -m pytest -q tests: 10 passed, bash -n tools/run_e008_r003.sh

> 代码应直接修改当前 Workspace 绑定仓库中的实际模块目录；只有仓库已有独立 launcher/adapter 目录时才使用它。工具不要求新建 codespace、实验索引或 runner 目录，科研逻辑不得为了登记 Run 而复制一份。

## 4. 运行与 Experiment Steward
- command: `cd /defaultShare/archive/liuwenchu/projects/IPLoc && bash mechanism/iplocid/tools/run_e008_r003_trials.sh`
- commit: ``
- workspace: 02
- tmux: incontext-E-008-003
- log: /defaultShare/archive/liuwenchu/projects/IPLoc/experiments/E-008/003/logs/train.log
- output: /defaultShare/archive/liuwenchu/projects/IPLoc/experiments/E-008/003/outputs
- Steward 摘要：尚未启动；浏览器不会自动启动 Extension

## 5. 关键结果
（程序完成后登记具体数值、比较对象和结果文件。）

## 6. 结果分析
（程序结束后由 pi 与研究者分析，Outbox completed 不等于科研分析完成。）

## 简短局限
这是在按历史IoU extremes预固定的条件cohort上的OOD attention干预；不能估计总体性能，也不能证明identity理解或唯一因果电路。逐token生成的KV-cache row与teacher replay full-sequence row不同，必须先做n4 exact replay/online一致性smoke。

<details><summary>历史兼容字段与补充执行信息</summary>

```json
{
  "implementation_summary": "新增自回归自然生成三条件pipeline；生成时用当前decode row=-1并按KV key length派生确定性置换，直到首个右方括号停止；逐样本保存文本、bbox、IoU和rewrite audit。",
  "implementation_details": "baseline卸载hook；identity/query_permuted安装softmax后、dropout前、A@V前hook。query span由prompt双图token定位；四个main4 heads；无合法bbox的full IoU记0。",
  "integrity_gates": "111×3条件均须有显式record或失败原因；identity与baseline逐token完全一致，否则停止；每次rewrite保持query-span mass、值多重集、entropy与span外值；仅在bbox前缀施加；所有IoU finite且[0,1]，无合法bbox按预注册规则记0。",
  "acceptance_criteria": "写出333条生成record、逐样本三条件文本/bbox/IoU、配对metrics.json和代表样本操作拼图；通过identity与保持性门禁后报告IoU差，结果停在analysis_pending，不自动形成Claim。",
  "claim_boundary": "最多说明在当前模型、main4 heads和预固定111样本上，query-span attention空间置换对自然生成bbox IoU的影响；不推广到其他heads、模型或数据分布。"
}
```

</details>

## 自由笔记（Obsidian）

这里可记录过程观察；结构化更新不会覆盖本节。
