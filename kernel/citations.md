# Citations (kernel truth)

Each entry: citekey -> bibliographic + provenance. content_hash is immutable once set.
Use ingest.py to add entries (it hashes the PDF automatically).
Never invent an entry by hand; if a paper can't be fetched, mark it [UNVERIFIED] with a note, no hash.